#------------------------------------------------------------------------------
# <auto-generated>
#     This code was generated by a tool.
#
#     Changes to this file may cause incorrect behavior and will be lost if
#     the code is regenerated.
# </auto-generated>
#------------------------------------------------------------------------------
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ticketproviders.ticketing.example.io
spec:
  group: ticketing.example.io
  names:
    kind: TicketProvider
    plural: ticketproviders
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TicketProvider contains the configuration information for handling an class of Ticket resources.
          The Ticket.spec.providerClass property should match exactly one TicketProvider.metadata.name to be processed.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: |-
              Spec is the configured state of the provider. It includes the provider type and any provider-specific
              properties and connection credentials.
            properties:
              type:
                description: |-
                  Identifies which backend work item system tracking system is
                  in effect. This imaginary example supports `azure` and `github` as
                  the backend for ticket tracking.
                type: string
              azure:
                description: Settings and credentials specific to the Azure DevOps work item tracking system.
                properties:
                  organization:
                    description: The Azure DevOps organization account name.
                    type: string
                  project:
                    description: The Azure DevOps project name used for Work Item tracking.
                    type: string
                  areaPath:
                    description: The Azure DevOps area path for ticket creation.
                    type: string
                  secretName:
                    description: The Kubernetes Secret metadata.name containing connection credentials.
                    type: string
                type: object
              github:
                description: Settings and credentials specific to the GitHub issue tracking system.
                properties:
                  organization:
                    description: The GitHub organiation name.
                    type: string
                  repository:
                    description: The GitHub repository name used for Issue tracking.
                    type: string
                  secretName:
                    description: The Kubernetes Secret metadata.name containing connection credentials.
                    type: string
                type: object
            type: object
          status:
            description: Status is the current state of the provider.
            properties:
              online:
                description: |-
                  Online is `true` when the backend provider is configured correctly
                  and connected.
                type: boolean
              message:
                description: |-
                  When Online is `false` Message contains the most recent error description
                  or exception message.
                type: string
            type: object
        title: V1alpha1TicketProvider
        type: object
    served: true
    storage: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: tickets.ticketing.example.io
spec:
  group: ticketing.example.io
  names:
    kind: Ticket
    plural: tickets
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          A Ticket represents a tracking record created in a work item database. For example, a
          microservice which has observed a low-priority condition that requires human interventions could create
          Ticket to be routed to the appropriate on-call staff. Is not intended to replace metric-based alerts
          for real-time notification.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: |-
              Spec contains the desired state of a work item that should be created in a related
              tracking database, e.g. GitHub Issues or AzureDevOps WorkItems.
            properties:
              providerClass:
                description: |-
                  ProviderClass must match the Metadata.Name of a TicketProvider in the cluster
                  in order to be processed.
                type: string
              severity:
                description: Severity is a system-specific value, e.g. high, low, or a string representation of a numeric value.
                type: string
              title:
                description: Contains the summary of the issue that is being reported.
                type: string
            type: object
          status:
            description: Status contains the current state of the work item as it stands in the tracking database.
            properties:
              uniqueId:
                description: Unique ticket identifier assigned by provider backend system.
                type: string
              workflowState:
                description: A human-readable state value of the ticket determined by the work item tracking provider.
                type: string
              webUrl:
                description: |-
                  Browser location which may be used to navigate to ticket on the
                  provider's user interface.
                type: string
              contactInfo:
                description: |-
                  Email address or other information to contact the person assigned to this
                  ticket directly.
                type: string
            type: object
        title: V1alpha1Ticket
        type: object
    served: true
    storage: true
